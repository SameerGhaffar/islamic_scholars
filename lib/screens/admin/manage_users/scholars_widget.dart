import 'package:flutter/material.dart';

class ScholarsWidget extends StatelessWidget {
  const ScholarsWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Scholars"),
    );
  }
}
